<h1>Buildings</h1>
<% if current_user %>
  <ul>
  <% current_user.buildings.each do |building| %>
    <li><%= link_to "#{building.name}" %>#, #user_building_path(building) %></li>
  <% end %>
  </ul>
  
<% end %>
